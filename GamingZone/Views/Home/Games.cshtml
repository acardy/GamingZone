@model IEnumerable<GamingZone.Models.Lobby>
@{
    ViewBag.Title = "Chat";
}

<h2>Chat</h2>
<p>Select a room to start.</p>
<p>Cribbage</p>
<ul>
    @foreach (var item in Model)
    {
        <li>@Html.ActionLink(item.Name, "Lobby", new { lobbyId = item.Id })</li>
    }
</ul>
